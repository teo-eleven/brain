# Obsidian — stare volatila de UI, se schimba la fiecare click
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.obsidian/plugins/*/data.json

# Backupurile facute de graph-view.ps1 inainte sa rescrie configul
.obsidian/*.bak

# Cosul de gunoi al vaultului
.trash/

# Date de la munca (Leviatan) - raman LOCAL, nu pe contul personal de GitHub.
# Contin nume de colegi si sedinte interne. Canvas-ul le afiseaza, deci iese si el.
scripts/teams-cache.json
Azi.canvas

# Jurnalul brut de evenimente (scripts/collector). Se rescrie zilnic si e doar
# sursa din care se genereaza vederea din inbox/, deci in istoric ar fi zgomot.
# Ramane LOCAL si pentru ca listeaza cai si nume de fisiere din repo-urile de
# serviciu, exact ca teams-cache.json de mai sus.
.events/

# Windows / macOS
Thumbs.db
desktop.ini
.DS_Store
